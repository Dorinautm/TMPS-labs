export default class Video{
    playMedia(name:string, videoTrack:string, audioTrack:string){
        console.log("Playing the sound from the file: " + name + 
        " ,videoTrack: " + videoTrack + " , audioTrack: " + audioTrack)
    }
}